# The pipeline needs four packages. The mock accounting API in accounting_api.py
# needs none of them — it runs on the standard library, exactly as distributed.
anthropic>=1.0.0,<2         # vision extraction; messages.parse() for schema-validated output
pydantic>=2.13,<3           # the extraction contract in ingest/schema.py
python-dotenv>=1.2,<2       # loads ANTHROPIC_API_KEY from .env
requests>=2.32,<3           # the accounting API client

# Development only — not needed to run the pipeline.
pytest>=9.0                 # unit tests for the normalizers and the validation gate
